// Unieke issue's

// #link to working site is missing
// # DOn't envoke strict mode on global scope --> Per functie defineren (iffe)


// Week 2:

Ajax = XMLHttpRequest();

xhttp.open("GET", "ajax_info.txt", true);
xhttp.send();

var request = new XMLHttpRequest(); // Object wordt in de variabele opgeslagen daarna kun je de properties hiervan aanroepen

request.open('GET', '/my/url', true);


/*What's an API?
Application program interfase.
Extra datalaag tussen je applicatie en het internet


Why are there API's


Routing
google --> vanilla js routing

*/